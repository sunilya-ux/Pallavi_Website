// AssignmentPanel is pending a redesign for the group-based assignment schema.
// The previous implementation relied on tool_id which no longer exists on assignments.
// Client-facing assignment display will be rebuilt in the next phase.
export default function AssignmentPanel(_props: { clientId: string }) {
  return null;
}
